import { Router } from "express";
// import {getRoles,createRole,updateRole,deleteRole,assignPermissions,} from "./roles.controller";
// import { authenticate } from "../../middlewares/auth.middleware";
// import { authorize } from "../../middlewares/role.middleware";

const router = Router();

// router.use(authenticate);
//
// router.get("/", authorize("ADMIN"), getRoles);
// router.post("/", authorize("ADMIN"), createRole);
// router.put("/:id", authorize("ADMIN"), updateRole);
// router.delete("/:id", authorize("ADMIN"), deleteRole);
// router.post("/:id/permissions", authorize("ADMIN"), assignPermissions);

export default router;
